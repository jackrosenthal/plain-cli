- [x] Add all dependencies to `go.mod` — run `go get` for kong, lipgloss, bubbletea, bubbles, coder/websocket, BurntSushi/toml, and golang.org/x/crypto; `go.sum` is present; `go build ./...` succeeds (there is no application code yet, only the module file)
- [x] Implement `internal/config/config.go` — defines `Config` struct with `Host`, `Token`, `ClientID` string fields and TOML tags; `Load()` resolves `$XDG_CONFIG_HOME/plain/config.toml` (falling back to `~/.config/plain/config.toml`), parses TOML, and returns zero-value `Config` (not an error) if the file does not exist; `Save()` marshals the struct to TOML and writes to the resolved path, creating parent directories as needed; if `ClientID` is empty after `Load()`, `Load()` generates a random UUID using `crypto/rand`, sets it on the struct, and calls `Save()` before returning
- [x] Implement `internal/client/crypto.go` — `DeriveSessionKey(token string) ([]byte, error)` base64-decodes the token and returns the first 32 bytes; `DeriveLoginKey(password string) []byte` computes `sha512_hex(password)` and returns the first 32 ASCII characters as a byte slice; `Encrypt(key, plaintext []byte) ([]byte, error)` generates a 24-byte random nonce, encrypts with XChaCha20-Poly1305, and returns `[nonce || ciphertext+tag]`; `Decrypt(key, ciphertext []byte) ([]byte, error)` splits the 24-byte nonce and decrypts; `WrapGraphQL(payload []byte, serverTimeMS int64) []byte` produces `"<unix_ms>|<16_char_hex_nonce>|<payload>"` with a fresh random 8-byte nonce hex-encoded to 16 chars
- [x] Implement `internal/client/auth.go` — `CheckToken(ctx, host, clientID string, sessionKey []byte) (valid bool, err error)` POSTs to `/init` as `multipart/form-data` with `c-id` header, sending an encrypted random UUID body if sessionKey is non-nil or an empty body otherwise; returns `(true, nil)` on 200+empty body, `(false, nil)` on 200+non-empty body, and an error on 403 or network failure; `Login(ctx, host, clientID, password string) (token string, err error)` opens a WebSocket to `ws[s]://<host>?cid=<clientID>&auth=1`, derives the login key from the password (if password is empty string the sha512 hash of an empty string is used), sends one binary frame containing the JSON auth payload (sha512_hex of password, hardcoded browser/OS fields) encrypted with the login key, then reads frames in a loop: a `{"status":"PENDING"}` frame causes the function to call an optional `onPending func()` callback (if non-nil) exactly once and then continue reading; a `{"token":...}` frame returns the token; a socket close with non-empty reason returns it as an error
- [x] Implement `internal/client/client.go` — defines `Client` struct holding `Host`, `ClientID` strings, `SessionKey []byte`, server time offset `int64`, and an `http.Client` with 30-second timeout; `NewClient(host, clientID, token string) (*Client, error)` derives the session key and returns a Client; `FetchServerTimeOffset(ctx) error` GETs the app root page, parses `window.__SERVER_TIME__` from the HTML, and stores the offset between server and local clock on the struct; `GraphQL(ctx, query string, variables map[string]any, result any) error` computes current server time using the stored offset, wraps with `WrapGraphQL`, encrypts, POSTs to `/graphql` as `multipart/form-data` with `c-id` header, decrypts the response body, and unmarshals into `result`; returns typed `ErrUnauthorized` on 401, `ErrForbidden` on 403, and `ErrTimeout` on timeout
- [x] Implement `internal/client/fileid.go` — `EncodeFileID(sessionKey []byte, path, mediaID string) (string, error)` encrypts `path` as a plain string when `mediaID` is empty, or `{"path":...,"mediaId":...}` JSON when non-empty, and returns the standard base64 encoding of the ciphertext; `DownloadFile(ctx, c *Client, path, mediaID string) (io.ReadCloser, error)` builds the fileId and calls `GET /fs?id=<fileId>&dl=1` with the `c-id` header, returning the response body
- [x] Implement `internal/client/upload.go` — `Upload(ctx, c *Client, localPath, remotePath string, progressFn func(done, total int64)) error` reads the local file, computes its SHA-256 hash to derive a stable `fileId`, calls the `uploadedChunks` GraphQL query to find already-received chunk indices, splits the file into ~1 MB chunks, POSTs remaining chunks to `/upload_chunk` as `multipart/form-data` (encrypted `info` field + raw `file` field) with up to 3 concurrent goroutines, calls `progressFn` after each chunk completes, and finally calls the `mergeChunks` GraphQL mutation with `isAppFile: false`
- [x] Implement `internal/client/events.go` — defines typed event structs for all 19 event types in the spec; defines `EventHandler` as a struct of optional function fields, one per event type; `ConnectEvents(ctx, c *Client, h EventHandler) error` connects to `ws[s]://<host>?cid=<clientID>`, sends the clock-sync binary frame (`encrypt(sessionKey, unix_ms_string)`), then loops reading binary frames, splitting the event-type byte from the ciphertext, decrypting, unmarshaling, and dispatching to the matching handler field if non-nil; on disconnect reconnects with backoff starting at 1s, increasing by 1s per attempt, capped at 5s; returns only on context cancellation
- [x] Implement `internal/api/types.go` — defines Go structs mirroring every GraphQL type in the spec (`Tag`, `File`, `Image`, `Video`, `Audio`, `Note`, `Feed`, `FeedEntry`, `Message`, `MessageConversation`, `Contact`, `ContentItem`, `Call`, `Package`, `Notification`, `Bookmark`, `BookmarkGroup`, `ChatItem`, `ChatChannel`, `Peer`, `Mount`, `App`, `DeviceInfo`, `Battery`, `PomodoroToday`, `PomodoroSettings`, `ImageSearchStatus`, `PlaylistAudio`, `ScreenMirrorQuality`); defines string-typed `DataType`, `FileSortBy`, `MediaPlayMode`, `ScreenMirrorMode` enums with constants for each value and a `ToGraphQL()` method that maps lowercase CLI values to uppercase GraphQL values
- [x] Implement `internal/api/queries.go` — defines unexported string constants for every query in the spec: `queryApp`, `queryDeviceInfo`, `queryBattery`, `queryPeers`, `queryMounts`, `queryFiles`, `queryRecentFiles`, `queryFileInfo`, `queryAppFiles`, `queryImages`, `queryVideos`, `queryAudios`, `queryMediaBuckets`, `queryTags`, `querySMS`, `querySMSConversations`, `queryContacts`, `queryContactSources`, `queryCalls`, `queryNotes`, `queryNote`, `queryFeeds`, `queryFeedEntries`, `queryFeedEntry`, `queryPackages`, `queryPackageStatuses`, `queryNotifications`, `queryBookmarks`, `queryBookmarkGroups`, `queryScreenMirror`, `queryPomodoroToday`, `queryPomodoroSettings`, `queryChatItems`, `queryChatChannels`, `queryUploadedChunks`, `queryImageSearchStatus`
- [x] Implement `internal/api/mutations.go` — defines unexported string constants for every mutation in the spec, following the same naming convention as queries.go
- [x] Implement `internal/output/output.go`, `internal/output/json.go`, `internal/output/plain.go`, and `internal/output/table.go` — `output.go` defines `Format` type with `FormatTable`, `FormatJSON`, `FormatPlain` constants and a `Printer` interface with `Print(v any) error` and `PrintList(v any) error` methods, plus `New(format Format, w io.Writer) Printer`; `json.go` marshals to indented JSON; `plain.go` reflects over struct fields and writes `key: value` lines with blank lines between records in `PrintList`; `table.go` uses lipgloss to render a styled table with bold column headers derived from struct field names, alternating row shading, and a two-column key/value layout for single records via `Print`
- [x] Implement `main.go`, `cmd/root.go`, and empty command struct stubs for every domain — `main.go` is ~10 lines: constructs the Kong instance, calls `kong.Parse()`, calls `Run()`, and exits 1 on error; `cmd/root.go` defines the `CLI` struct with `--host`, `--token`, `--client-id`, `--output` global flags and a field for each subcommand domain, plus `AfterApply(k *kong.Kong)` which loads config, merges flag/env overrides, constructs `*client.Client`, calls `FetchServerTimeOffset`, constructs an `output.Printer`, and calls `k.Bind()` with both (skipping client construction when `auth login` or `auth status` is the selected command); each of `cmd/auth.go`, `cmd/device.go`, `cmd/files.go`, `cmd/images.go`, `cmd/videos.go`, `cmd/audio.go`, `cmd/sms.go`, `cmd/contacts.go`, `cmd/calls.go`, `cmd/notes.go`, `cmd/feeds.go`, `cmd/packages.go`, `cmd/notifications.go`, `cmd/bookmarks.go`, `cmd/chat.go`, `cmd/tags.go`, `cmd/screen.go`, `cmd/pomodoro.go`, `cmd/clipboard.go` contains only the top-level command struct(s) with Kong tags and no `Run` methods; `go build ./...` succeeds and `./plain --help` prints the full command tree
- [x] Implement `cmd/auth.go` — `auth login` (no flags) calls `Login()` with an empty password and passes an `onPending` callback that prints "Waiting for device confirmation..." to stderr; `auth login --password` uses a bubbletea bubbles masked text-input to collect the password, then calls `Login()` with the same `onPending` callback; both paths write the returned token to config via `config.Save()` and print a success confirmation; `auth status` calls `CheckToken` and prints whether the token is valid or expired; all output through Printer
- [x] Implement `cmd/device.go` — `device info` runs the `deviceInfo` query and prints `DeviceInfo`; `device battery` runs `battery` and prints `Battery`; `device app` runs `app` and prints `App`; `device peers` runs `peers` and prints `[]Peer`; `device mounts` runs `mounts` and prints `[]Mount` with byte sizes formatted as human-readable strings (KB/MB/GB); `device relaunch` runs `relaunchApp` and prints a confirmation; all output through Printer
- [x] Implement `cmd/files.go` — `files ls` auto-paginates `files` (respecting optional `--limit`/`--offset`) and prints `[]File`; `files recent` runs `recentFiles`; `files info` runs `fileInfo`; `files mkdir` runs `createDir`; `files write` reads `--content` or stdin and runs `writeTextFile`; `files rename` runs `renameFile`; `files copy` runs `copyFile`; `files move` runs `moveFile`; `files delete` runs `deleteFiles`; `files download` calls `DownloadFile`, writes to `--out` path or stdout, shows a lipgloss progress bar when writing to a file (suppressed for `--output json|plain`); `files upload` calls `Upload()` with a progress callback rendering a lipgloss progress bar (suppressed for `--output json|plain`); `files favorites ls` reads `favoriteFolders` from the `app` query; `files favorites add` runs `addFavoriteFolder`; `files favorites remove` runs `removeFavoriteFolder`; `files favorites alias` runs `setFavoriteFolderAlias`; all output through Printer
- [x] Implement `cmd/images.go` — `images ls` auto-paginates `images`; `images buckets` runs `mediaBuckets(type: IMAGE)`; `images download` calls `DownloadFile` with a progress bar; `images trash` runs `trashMediaItems(IMAGE, query)`; `images restore` runs `restoreMediaItems(IMAGE, query)`; `images delete` runs `deleteMediaItems(IMAGE, query)`; `images search status` runs `imageSearchStatus`; `images search enable` runs `enableImageSearch`; `images search disable` runs `disableImageSearch`; `images search index` runs `startImageIndex` with optional `--force`; all output through Printer
- [x] Implement `cmd/videos.go` — `videos ls` auto-paginates `videos`; `videos buckets` runs `mediaBuckets(type: VIDEO)`; `videos download` calls `DownloadFile` with a progress bar; `videos trash`, `videos restore`, `videos delete` run the media management mutations with `type: VIDEO`; all output through Printer
- [x] Implement `cmd/audio.go` — `audio ls` auto-paginates `audios`; `audio play` runs `playAudio` and prints the now-playing track; `audio mode` maps `order|shuffle|repeat|repeat-one` to `MediaPlayMode` and runs `updateAudioPlayMode`; `audio trash`, `audio restore`, `audio delete` run the media management mutations with `type: AUDIO`; `audio playlist ls` reads `audios` from the `app` query; `audio playlist add` runs `addPlaylistAudios`; `audio playlist remove` runs `deletePlaylistAudio`; `audio playlist clear` runs `clearAudioPlaylist`; `audio playlist reorder` runs `reorderPlaylistAudios`; all output through Printer
- [x] Implement `cmd/sms.go` — `sms ls` auto-paginates `sms`; `sms conversations` auto-paginates `smsConversations`; `sms send` runs `sendSms` and prints a confirmation; `sms send-mms` runs `sendMms` with `--attachments` (defaults to empty slice) and required `--thread-id`; all output through Printer
- [x] Implement `cmd/contacts.go` — `contacts ls` auto-paginates `contacts`; `contacts sources` runs `contactSources`; `contacts delete` runs `deleteContacts`; all output through Printer
- [x] Implement `cmd/calls.go` — `calls ls` auto-paginates `calls` and labels the `type` field as `incoming`, `outgoing`, or `missed` (1/2/3) in table and plain output; `calls call` runs the `call` mutation; `calls delete` runs `deleteCalls`; all output through Printer
- [x] Implement `cmd/notes.go` — `notes ls` auto-paginates `notes` (id, title, dates, tag names); `notes get` runs `note(id)` and prints the full note including content; `notes save` reads `--content` or stdin, uses `--id` if provided otherwise generates a new UUID, runs `saveNote`, and prints the saved `Note`; `notes trash` runs `trashNotes`; `notes restore` runs `restoreNotes`; `notes delete` runs `deleteNotes`; `notes export` runs `exportNotes`; all output through Printer
- [x] Implement `cmd/feeds.go` — `feeds ls` runs `feeds`; `feeds add` runs `createFeed`; `feeds update` runs `updateFeed`; `feeds delete` runs `deleteFeed`; `feeds sync` runs `syncFeeds` with or without `--id`; `feeds import` reads a local OPML file and runs `importFeeds`; `feeds export` runs `exportFeeds` and writes the OPML string to stdout regardless of `--output` mode; `feeds entries ls` auto-paginates `feedEntries`; `feeds entries get` runs `feedEntry(id)` and includes the nested feed; `feeds entries delete` runs `deleteFeedEntries`; `feeds entries save-to-notes` runs `saveFeedEntriesToNotes`; all other output through Printer
- [ ] Implement `cmd/packages.go` — `packages ls` auto-paginates `packages`; `packages install` runs `installPackage` and prints the result including whether the package is new; `packages uninstall` runs `uninstallPackages`; all output through Printer
- [ ] Implement `cmd/notifications.go` — `notifications ls` runs `notifications`; `notifications cancel` runs `cancelNotifications`; `notifications reply` runs `replyNotification` with `--action-index` and `--text`; all output through Printer
- [ ] Implement `cmd/bookmarks.go` — `bookmarks ls` runs `bookmarks`; `bookmarks add` runs `addBookmarks`; `bookmarks update` fetches the existing bookmark first then merges only the provided optional flags before running `updateBookmark`; `bookmarks delete` runs `deleteBookmarks`; `bookmarks groups ls` runs `bookmarkGroups`; `bookmarks groups create` runs `createBookmarkGroup`; `bookmarks groups update` runs `updateBookmarkGroup`; `bookmarks groups delete` runs `deleteBookmarkGroup`; all output through Printer
- [ ] Implement `cmd/chat.go` — `chat channels ls` runs `chatChannels`; `chat channels create` runs `createChatChannel`; `chat channels update` runs `updateChatChannel`; `chat channels delete` runs `deleteChatChannel`; `chat channels leave` runs `leaveChatChannel`; `chat channels invite accept` runs `acceptChatChannelInvite`; `chat channels invite decline` runs `declineChatChannelInvite`; `chat channels members add` runs `addChatChannelMember`; `chat channels members remove` runs `removeChatChannelMember`; `chat messages` runs `chatItems(id)`; `chat send` runs `sendChatItem`; `chat delete` runs `deleteChatItem`; all output through Printer
- [ ] Implement `cmd/tags.go` — `tags ls` runs `tags(type)` with CLI-to-GraphQL type mapping; `tags create` runs `createTag`; `tags update` runs `updateTag`; `tags delete` runs `deleteTag`; `tags add` parses comma-separated `--tags` and runs `addToTags`; `tags remove` runs `removeFromTags`; all output through Printer
- [ ] Implement `cmd/screen.go` — `screen status` runs the three screen mirror queries together and prints as a single record; `screen start` runs `startScreenMirror(audio: bool)`; `screen stop` runs `stopScreenMirror`; `screen quality` maps `auto|hd|smooth` to `ScreenMirrorMode` and runs `updateScreenMirrorQuality`; all output through Printer
- [ ] Implement `cmd/pomodoro.go` — `pomodoro status` runs `pomodoroToday`; `pomodoro settings` runs `pomodoroSettings`; `pomodoro start` uses `--time-left` if provided, otherwise fetches `pomodoroSettings` first and uses `workDuration` as the default, then runs `startPomodoro`; `pomodoro stop` runs `stopPomodoro`; `pomodoro pause` runs `pausePomodoro`; all output through Printer
- [ ] Implement `cmd/clipboard.go` — `clipboard set` runs `setClip(text)` and prints a confirmation via Printer
